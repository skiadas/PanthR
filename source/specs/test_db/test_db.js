/**
 * New node file
 */

 describe("A test to check database connection",function(var db) {
 
 // inquiring mongodb module 
 // load up db module that RJ made
 // create a test db 
 var db;
 
 beforeEach(function() {
 
 });
 
 afterEach(function() {
 
 });
 
 it("check whether the database is available", function() {
 
 }); 
 
 });